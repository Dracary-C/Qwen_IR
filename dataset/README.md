# Dataset Assets

This directory keeps small Qwen fixed-sample manifests and copied few-shot target images used by the structured-prior prompt iteration scripts.

Large training/validation datasets remain external and are referenced from `config/train_sample.yml` and `config/test_batch.yml`, for example `/data/chenzt/Dataset/sample` and `/data/chenzt/Dataset/Qwen3VL/0611_sample`.
